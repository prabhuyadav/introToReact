import React from "react";
import ReactDOM from "react-dom";

const htmlElement = (
  <div>
    <h1>Hello React</h1>
    <p>This is React Introduction</p>
  </div>
);

ReactDOM.render(htmlElement, document.getElementById("root"));
